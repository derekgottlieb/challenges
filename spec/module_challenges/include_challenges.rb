require 'module_challenges/challenges'
# include
#   define a module MahMixin
#   define a class MahMixin::User
#     users have a name and an age
#     define the spaceship method on User
#     when the left user is younger, it returns -1
#     when the left user is older, it returns 1
#     when the left user is the same age, it returns 0
#   define a module MahMixin::Comparable
#     include it into your User class
#     define the < method based on the <=> method
#     define the > method based on the <=> method
#     define the <= method based on the <=> method
#     define the >= method based on the <=> method
#     define the == method based on the <=> method
